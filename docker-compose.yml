services:
  notifyhub:
    image: notifyhub
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - notifyhub-data:/app/data
    environment:
      - DATABASE_URL=sqlite:///data/notifyhub.db
    restart: unless-stopped

volumes:
  notifyhub-data:
